/*
let storeProducts= ["milk", "cheese", "cream", "eggs"]
let addOrDelete= prompt("Do you want to add or delete a product?")
if (addOrDelete == "Add") {
  let addedProduct=prompt("Which product would you like to add?")
  storeProducts.push(addedProduct)
  console.log(storeProducts)
}
else (addOrDelete= "delete) {
  let deletedProduct=prompt("Which product would you like to delete?")
  storeProducts.pop(deletedProduct)
  console.log(storeProducts)
  }
  */